# Copy to .env and replace the password before starting the database.
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5433
POSTGRES_DB=jp_doc_agent
POSTGRES_USER=jp_doc_agent
POSTGRES_PASSWORD=replace_with_a_random_password
